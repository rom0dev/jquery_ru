<nav class="blog-detail-nav desktop-only">
  <ul class="clearfix">
    <li <% if (page.base === "") { %> class="selected" <% } %>">
      <a href="<%= config.root %>">Главная</a>
    </li>

    <li <% if (page.base === 'archives/') { %> class="selected" <% } %>>
      <a href="<%= url_for(theme.archive_dir) %>">
        Постов <span class="counter"><%= site.posts.length %></span>
      </a>
    </li>

    <li <% if (page.type === 'categories') { %> class="selected" <% } %>>
      <a href="<%= url_for(theme.category_dir) %>">
        Категорий <span class="counter"><%= site.categories.length %></span>
      </a>
    </li>

    <li <% if( page.type === 'tags') { %> class="selected" <% } %>>
      <a href="<%= url_for(theme.tag_dir) %>">
        Тэгов <span class="counter"><%= site.tags.length %></span>
      </a>
    </li>
  </ul>
</nav>
